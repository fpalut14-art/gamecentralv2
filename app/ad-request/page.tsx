'use client';
import React,{useState}from'react';import{addDoc,collection}from'firebase/firestore';import{db}from'@/lib/firebase';
export default function AdRequestPage(){const[brand,setBrand]=useState('');const[title,setTitle]=useState('');const[slot,setSlot]=useState('premium');const[link,setLink]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();await addDoc(collection(db,'ads'),{brand,title,slot,link,status:'pending',createdAt:new Date().toISOString()});alert('Reklam başvurusu admin onayına gönderildi.');setBrand('');setTitle('');setSlot('premium');setLink('')}return <main style={{minHeight:'100vh',background:'#05060f',color:'white',display:'grid',placeItems:'center',padding:30}}><form onSubmit={submit} style={{width:'100%',maxWidth:560,padding:34,borderRadius:24,background:'#0f172a',display:'grid',gap:16}}><h1 style={{color:'#ffd400'}}>Reklam Başvurusu</h1><input placeholder="Marka" value={brand} onChange={e=>setBrand(e.target.value)} required/><input placeholder="Reklam başlığı" value={title} onChange={e=>setTitle(e.target.value)} required/><select value={slot} onChange={e=>setSlot(e.target.value)}><option value="premium">Premium Slider</option><option value="right-banner">Sağ Banner</option><option value="partner-slot">Partner Slot</option></select><input placeholder="Link" value={link} onChange={e=>setLink(e.target.value)}/><button style={{height:54,background:'#ffd400',fontWeight:900,borderRadius:12,border:0}}>GÖNDER</button></form></main>}
