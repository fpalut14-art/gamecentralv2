'use client';
import React,{useState}from'react';import Link from'next/link';import{useRouter}from'next/navigation';import{createUserWithEmailAndPassword}from'firebase/auth';import{doc,setDoc}from'firebase/firestore';import{auth,db}from'@/lib/firebase';
const page={minHeight:'100vh',background:'#05060f',display:'grid',placeItems:'center',padding:30,color:'white'} as React.CSSProperties;const box={width:'100%',maxWidth:500,padding:34,borderRadius:26,background:'#0f172a',border:'1px solid rgba(255,255,255,.09)'} as React.CSSProperties;const input={height:54,borderRadius:14,border:'1px solid rgba(255,255,255,.09)',background:'#111827',padding:'0 16px',color:'white'} as React.CSSProperties;const btn={height:54,border:0,borderRadius:14,background:'#ffd400',color:'#050505',fontWeight:900,cursor:'pointer'} as React.CSSProperties;
export default function RegisterPage(){const router=useRouter();const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');async function handleRegister(e:React.FormEvent<HTMLFormElement>){e.preventDefault();try{const result=await createUserWithEmailAndPassword(auth,email,password);await setDoc(doc(db,'users',result.user.uid),{name,email,role:'user',sellerStatus:'none',banned:false,createdAt:new Date().toISOString()});alert('Kayıt başarılı.');router.push('/login')}catch(error:any){alert('Kayıt başarısız: '+error.message)}}return <main style={page}><div style={box}><Link href="/" style={{color:'white',fontWeight:900,fontSize:28}}>GAME<span style={{color:'#ffd400'}}>CENTRAL</span></Link><h1 style={{color:'#ffd400'}}>Kayıt Ol</h1><form onSubmit={handleRegister} style={{display:'grid',gap:16,marginTop:26}}><input placeholder="Ad / Kullanıcı adı" value={name} onChange={e=>setName(e.target.value)} style={input} required/><input type="email" placeholder="E-posta" value={email} onChange={e=>setEmail(e.target.value)} style={input} required/><input type="password" placeholder="Şifre" value={password} onChange={e=>setPassword(e.target.value)} style={input} required/><button style={btn}>KAYIT OL</button></form><Link href="/login" style={{display:'inline-block',marginTop:18,color:'#38bdf8',fontWeight:800}}>Hesabım var</Link></div></main>}
